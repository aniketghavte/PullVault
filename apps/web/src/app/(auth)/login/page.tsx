export default function LoginPage() {
  return (
    <div className="mx-auto max-w-md space-y-4">
      <h1 className="text-2xl font-semibold">Sign in</h1>
      <p className="text-gray-400">Auth UI will live here (Supabase email + password).</p>
    </div>
  );
}
